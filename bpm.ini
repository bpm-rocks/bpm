[package]
name=bpm
version=1.1.5
description=Bash package manager.

[dependencies]
assign=*
ini=*
is=*
log=*
path=*
strict=*

[devDependencies]
unittest=*

[install]
bpm=bpm

[scripts]
build=util/build > bpm; chmod 755 bpm
clean=rm -rf bpm
test=unittest test/**/*.test

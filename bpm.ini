[package]
name=bpm
version=1.0.0
description=Bash package manager.

[dependencies]
0=assign
1=ini
2=is
3=log

[devDependencies]
0=strict
1=unittest

[install]
0=bpm

[scripts]
build=util/build > bpm; chmod 755 bpm
clean=rm -rf bpm
test=unittest test/*.test test/**/*.test

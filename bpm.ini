[package]
name=bpm
version=1.0.0
description=Bash package manager.

[dependencies]
assign=*
ini=*
is=*
log=*
path=*

[devDependencies]
strict=*
unittest=*

[install]
bpm=bpm

[scripts]
build=util/build > bpm; chmod 755 bpm
clean=rm -rf bpm
test=unittest test/*.test test/**/*.test
